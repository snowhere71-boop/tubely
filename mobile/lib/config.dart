// Point this at your deployed backend before building the APK — see the
// README for how to deploy the backend (Render's free tier works well).
//
// The default below only works when testing on an Android *emulator* with
// the backend running on your own computer — 10.0.2.2 is the emulator's
// special alias for your computer's localhost. It will NOT work on a real
// phone, since a phone can't reach your computer's localhost over the
// internet.
const String apiBaseUrl = 'http://10.0.2.2:5000/api';
